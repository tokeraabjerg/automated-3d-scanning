version: '3.8'

services:
  flask-app:
    build: .
    ports:
      - "5001:5001"
    environment:
      - FLASK_APP=app.py
      - FLASK_RUN_HOST=0.0.0.0
    privileged: true  # Run with elevated privileges if necessary